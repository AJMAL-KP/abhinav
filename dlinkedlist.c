#include<stdio.h>
#include<stdlib.h>

struct node{
    int data;
    struct node * next;
};
struct node *head1=NULL;
struct node * head2=NULL;
void insertBeginning(struct node **head_ref,value){
    struct node * newNode= (struct node *) malloc(sizeof(struct node));
    if(*head==NULL){

    }
}